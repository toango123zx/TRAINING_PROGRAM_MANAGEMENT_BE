model GV {
    id_GV        String  @id @default(uuid())
    id_user      String  @unique
    description  String?
    degree       String?
    work_address String
    status       Int     @default(0)

    user    User    @relation(fields: [id_user], references: [id_user])
    classes Class[]
}
